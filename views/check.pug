extends layout
block content
    form(action="check" method="post")
        .card
            h1 阿金的生日
            if error
                .error 欸，太不了解了吧?!
            .input
                input.input__field.input__field--date(id="birthdayInput", name="birthday", type="date", required, autofocus)
                span.input__label.input__label--date 請輸入阿金的出生西元年
            .button-group
                button(type="submit") 確認


